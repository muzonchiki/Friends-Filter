## Project friends filter (vk.api)

### Доступные команды:
* `yarn install` - установить зависимости
* `yarn start` - запустить встроенный сервер и следить за изменениями файлов
* `yarn build` -  собрать проект в папку `build`